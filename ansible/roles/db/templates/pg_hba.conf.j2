# local
local   all   all                     peer
host    all   all   127.0.0.1/32      md5
host    all   all   ::1/128           md5

# authentik DB from SSO VM only
{% for cidr in pg_allowed_cidrs %}
host    {{ pg_db_authentik }}   {{ pg_user_authentik }}   {{ cidr }}   md5
{% endfor %}
